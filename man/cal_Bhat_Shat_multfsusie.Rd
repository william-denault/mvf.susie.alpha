% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/computational_routine.R
\name{cal_Bhat_Shat_multfsusie}
\alias{cal_Bhat_Shat_multfsusie}
\title{Compute Bhat / Shat across all multfSuSiE modalities}
\usage{
cal_Bhat_Shat_multfsusie(
  Y,
  X,
  sigma2,
  low_trait = NULL,
  ind_analysis = NULL,
  v1 = NULL,
  list_indx_lst = NULL,
  ...
)
}
\arguments{
\item{Y}{named list with optional Y_u (N x K_u, univariate traits)
and optional Y_f (length-M list of N x T_m wavelet
coefficient matrices).}

\item{X}{N x p predictor matrix.}

\item{sigma2}{multfsusie.obj$sigma2 — a list with components
$sd_u  (length K_u, per univariate trait), and/or
$sd_f  (numeric vector of length M, with exactly one
       scalar residual variance per modality).}

\item{low_trait}{list of low-count masks: $low_u (vec of trait indices to
mask) and $low_wc (length-M list of position-index vecs).}

\item{ind_analysis}{named list with $idx_u (length K_u of row vectors)
and $idx_f (length M of row vectors); missing or NULL
means "use all rows".}

\item{v1, }{list_indx_lst   ignored — kept for backward signature
compatibility.}
}
\value{
list(res_u = list(Bhat, Shat) | NULL,
             res_f = list of M lists(Bhat, Shat) | NULL)
}
\description{
Compute Bhat / Shat across all multfSuSiE modalities
}
